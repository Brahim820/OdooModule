<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="invoice_currency_helpan.list">
      <field name="name">invoice_currency_helpan list</field>
      <field name="model">invoice_currency_helpan.invoice_currency_helpan</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="invoice_currency_helpan.action_window">
      <field name="name">invoice_currency_helpan window</field>
      <field name="res_model">invoice_currency_helpan.invoice_currency_helpan</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="invoice_currency_helpan.action_server">
      <field name="name">invoice_currency_helpan server</field>
      <field name="model_id" ref="model_invoice_currency_helpan_invoice_currency_helpan"/>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="invoice_currency_helpan" id="invoice_currency_helpan.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="invoice_currency_helpan.menu_1" parent="invoice_currency_helpan.menu_root"/>
    <menuitem name="Menu 2" id="invoice_currency_helpan.menu_2" parent="invoice_currency_helpan.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="invoice_currency_helpan.menu_1_list" parent="invoice_currency_helpan.menu_1"
              action="invoice_currency_helpan.action_window"/>
    <menuitem name="Server to list" id="invoice_currency_helpan" parent="invoice_currency_helpan.menu_2"
              action="invoice_currency_helpan.action_server"/>
    -->
  </data>
</odoo>